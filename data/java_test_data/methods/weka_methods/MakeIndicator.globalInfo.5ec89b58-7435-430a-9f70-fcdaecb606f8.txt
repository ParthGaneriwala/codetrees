public String globalInfo() {
    return "A filter that creates a new dataset with a Boolean attribute " + "replacing a nominal attribute.  In the new dataset, a value of 1 is " + "assigned to an instance that exhibits a value in the given range of attribute " + "values, and a value of 0 is assigned to an instance that does not. The Boolean attribute is " + "coded as numeric by default.";
}

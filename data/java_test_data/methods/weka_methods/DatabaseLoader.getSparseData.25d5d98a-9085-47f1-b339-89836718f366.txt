@OptionMetadata(displayName = "Create sparse instances", description = "Return sparse " + "rather than normal instances", displayOrder = 6)
public boolean getSparseData() {
    return m_CreateSparseData;
}

public String globalInfo() {
    return "A simple instance filter that renames the relation, all attribute names " + "and all nominal attribute values. For exchanging sensitive " + "datasets. Leaves string and relational attributes untouched.";
}

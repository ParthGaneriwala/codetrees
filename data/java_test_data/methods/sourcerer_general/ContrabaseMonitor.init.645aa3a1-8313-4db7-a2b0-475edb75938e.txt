public void init() {
    this.setLayout(new BorderLayout());
    repository1 = new JButton("Repository1");
    repository2 = new JButton("Repository2");
    repository3 = new JButton("Repository3");
    sync = new JButton("Sync");
    tablePanel = new JPanel(new BorderLayout());
    repositoryPanel = new JPanel(new BorderLayout());
    eastPanel = new JPanel();
    southPanel = new JPanel(new GridLayout(1, 10));
    tableList = new JPanel();
    repositoryList = new JPanel();
    repositorySize = new JLabel("Repository Size :");
    tableSize = new JLabel("Table Size :");
    fileSize = new JLabel("File Size : ");
    tableListScrollPane = new JScrollPane(tableList);
    repositoryListScrollPane = new JScrollPane(repositoryList);
    classfication = new JTabbedPane(JTabbedPane.TOP, JTabbedPane.SCROLL_TAB_LAYOUT);
    repositoryPanel.add(BorderLayout.WEST, repositoryListScrollPane);
    tablePanel.add(BorderLayout.WEST, tableListScrollPane);
    classfication.addTab("Table", tablePanel);
    classfication.addTab("Repository", repositoryPanel);
    southPanel.add(repositorySize);
    southPanel.add(tableSize);
    southPanel.add(fileSize);
    eastPanel.add(sync);
    this.add(BorderLayout.CENTER, classfication);
    this.add(BorderLayout.EAST, eastPanel);
    this.add(BorderLayout.SOUTH, southPanel);
    // Test 부분임..
    tableList.add(new JButton("test1"));
    repositoryList.add(new JButton("test2"));
    String[] testField = { "age", "idid", "name" };
    String[][] testValue = { { "15", "10232", "test" }, { "20", "3023", "test2" } };
    JTable jt = new JTable(testValue, testField);
    JScrollPane jsp = new JScrollPane(jt);
    tablePanel.add(jsp);
    String[] test2Field = { "weigth", "id", "name" };
    String[][] test2Value = { { "55", "24242", "test4" }, { "244", "24244", "test5" } };
    JTable jt2 = new JTable(test2Value, test2Field);
    JScrollPane jsp2 = new JScrollPane(jt2);
    repositoryPanel.add(jsp2);
}

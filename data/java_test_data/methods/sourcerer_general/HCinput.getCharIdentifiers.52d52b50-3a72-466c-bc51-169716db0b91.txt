private void getCharIdentifiers() throws IOException {
    sendMsg = "Input Figure Name: ";
    System.out.print(sendMsg);
    recMsg = keyReader.readLine();
    FigToInput.CName = recMsg;
    sendMsg = "Input Figure Rank";
    System.out.println(sendMsg);
    recMsg = keyReader.readLine();
    FigToInput.Rank = rankFormatter(recMsg);
    sendMsg = "Input Figure Cost";
    FigToInput.Cost = numberFormatterRecursive(recMsg);
    sendMsg = "Collectors Number?";
    System.out.println(sendMsg);
    recMsg = keyReader.readLine();
    FigToInput.CNumber = recMsg;
    sendMsg = "Team Affiliation";
    System.out.println(sendMsg);
    recMsg = keyReader.readLine();
    FigToInput.Team = recMsg;
    sendMsg = "Range in squares";
    FigToInput.Range = numberFormatterRecursive(recMsg);
    sendMsg = "number of attack arrows";
    FigToInput.Arrows = numberFormatterRecursive(recMsg);
    sendMsg = "Can Figure Fly?";
    FigToInput.canFly = askCanDoRecursion();
    sendMsg = "Can Figure Swim?";
    FigToInput.canSwim = askCanDoRecursion();
    sendMsg = "Can Figure Transport?";
    FigToInput.canTransport = askCanDoRecursion();
    sendMsg = "Can Figure TransWing?";
    FigToInput.canTranswing = askCanDoRecursion();
    sendMsg = "Is Figure a Giant?";
    FigToInput.canGiant = askCanDoRecursion();
    sendMsg = "Is Figure a DoubleBase?";
    FigToInput.doubleBase = askCanDoRecursion();
    if (numberOfClicks > 12)
        FigToInput.LargeFig = true;
    moveClicksInput();
    attackClicksInput();
    defenseClicksInput();
    damageClicksInput();
}

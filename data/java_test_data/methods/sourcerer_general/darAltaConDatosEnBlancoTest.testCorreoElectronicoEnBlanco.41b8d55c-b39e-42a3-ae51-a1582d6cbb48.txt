public void testCorreoElectronicoEnBlanco() {
    // The setup should leave the modal dialog running on a
    // different thread. Thus we can access the dialog
    // throught the normal JFCUnit finders.
    List dialogs = new FrameFinder("Acceso a Consultaplus").findAll();
    assertEquals("Dialog not found:", 1, dialogs.size());
    Container campo_usuario = (Container) dialogs.get(0);
    Finder f3 = new ComponentFinder(JTextField.class);
    JTextField field3 = (JTextField) f3.find(campo_usuario, 0);
    assertNotNull("Could not find field", field3);
    m_helper.sendString(new StringEventData(this, field3, "admin"));
    Container campo_contrasea = (Container) dialogs.get(0);
    Finder f5 = new ComponentFinder(JTextField.class);
    JTextField field5 = (JTextField) f5.find(campo_contrasea, 1);
    assertNotNull("Could not find field", field5);
    m_helper.sendString(new StringEventData(this, field5, "admin"));
    Container boton_aceptar = (Container) dialogs.get(0);
    Finder f6 = new ComponentFinder(JLabel.class);
    JLabel field6 = (JLabel) f6.find(boton_aceptar, 0);
    assertNotNull("Could not find field", field6);
    f6 = new ComponentFinder(JLabel.class);
    JLabel aceptar = (JLabel) f6.find(boton_aceptar, 3);
    m_helper.enterClickAndLeave(new MouseEventData(this, aceptar));
    assertTrue("Unsuccessful exit:", m_normalExit);
    checkException();
    List dialogs1 = new DialogFinder("Crear Usuario").findAll();
    assertEquals("Dialog not found:", 1, dialogs.size());
    Container campo_new_usuario = (Container) dialogs1.get(0);
    Finder f7 = new ComponentFinder(JTextField.class);
    JTextField field7 = (JTextField) f7.find(campo_new_usuario, 1);
    assertNotNull("Could not find field", field7);
    m_helper.sendString(new StringEventData(this, field7, "Rocio"));
    Container campo_cont = (Container) dialogs1.get(0);
    Finder f8 = new ComponentFinder(JTextField.class);
    JTextField field8 = (JTextField) f8.find(campo_cont, 2);
    assertNotNull("Could not find field", field8);
    m_helper.sendString(new StringEventData(this, field8, "admin"));
    Container campo_confirmar = (Container) dialogs1.get(0);
    Finder f9 = new ComponentFinder(JTextField.class);
    JTextField field9 = (JTextField) f9.find(campo_confirmar, 3);
    assertNotNull("Could not find field", field9);
    m_helper.sendString(new StringEventData(this, field9, "admin"));
    Container campo_direc = (Container) dialogs1.get(0);
    Finder f10 = new ComponentFinder(JTextField.class);
    JTextField field10 = (JTextField) f10.find(campo_direc, 0);
    assertNotNull("Could not find field", field10);
    m_helper.sendString(new StringEventData(this, field10, ""));
    Container privi = (Container) dialogs1.get(0);
    Finder f11 = new ComponentFinder(JRadioButton.class);
    JRadioButton field11 = (JRadioButton) f11.find(privi, 0);
    assertNotNull("Could not find field", field11);
    f11 = new ComponentFinder(JRadioButton.class);
    JRadioButton med = (JRadioButton) f11.find(privi, 0);
    m_helper.enterClickAndLeave(new MouseEventData(this, med));
    assertTrue("Unsuccessful exit:", m_normalExit);
    checkException();
    Container boton_acep = (Container) dialogs1.get(0);
    Finder f12 = new ComponentFinder(JButton.class);
    JButton field12 = (JButton) f12.find(boton_acep, 0);
    assertNotNull("Could not find field", field12);
    f12 = new ComponentFinder(JButton.class);
    JButton acep = (JButton) f12.find(boton_acep, 0);
    m_helper.enterClickAndLeave(new MouseEventData(this, acep));
    assertTrue("Unsuccessful exit:", m_normalExit);
    checkException();
    List dialogs2 = new DialogFinder("Error: El formato del correo electronico no es valido").findAll();
    assertEquals("Dialog not found:", 1, dialogs2.size());
}

private void init() {
    totalTorqueField = new JTextField("  n/a");
    // sensitivityMultiplierModel = new DefaultComboBoxModel[9];
    // sensitivityMultiplierComboBoxes = new JComboBox[9];
    setBorder(BorderFactory.createLineBorder(Color.BLACK, 1));
    // LayoutManager layout = new FormLayout("left:pref, 4dlu, pref:grow, left:pref, pref:grow, 4dlu, 4dlu, pref:grow");
    LayoutManager layout = new GridLayout(1, 1);
    LayoutManager gridbagLayout = new GridBagLayout();
    // setLayout(gridbagLayout);
    setLayout(new GridLayout(1, 1));
    GridBagConstraints c = new GridBagConstraints();
    // c.fill = GridBagConstraints.RELATIVE;
    // c.gridwidth = GridBagConstraints.REMAINDER;
    c.weightx = 1.0;
    add(new JLabel("  Select ViewPoint:"));
    c.weightx = 2.0;
    viewPointComboBox = createViewPointComboBox();
    c.weightx = 1.0;
    add(viewPointComboBox);
    // add(new JLabel(""));
    add(new JLabel("  Select Sensitivity Scale:"));
    Vector<SensitivityMultiplier> sensitivityMultiplierVector = ViewPointFactory.getInstance().getSensitivityMultiplierVector();
    sensitivityScaleComboBox = createSensitivityComboBox(sensitivityMultiplierVector);
    c.weightx = 2.0;
    add(sensitivityScaleComboBox);
    add(new JLabel("    Total Torque for current layer:"));
    c.weightx = 1.0;
    add(totalTorqueField);
// c.gridwidth = GridBagConstraints.REMAINDER;
}

// public static MidiPlayer player;
@Override
public void initialize() {
    Application.aMainPlayer.stopMusic();
    Application.aMainPlayer.setSong(1);
    Application.aMainPlayer.loopMusic();
    // Define the postions of things
    aBackground = new Panel(new Coordinate(0, 0), new Coordinate(Application.WIN_SIZE_X, Application.WIN_SIZE_Y));
    aBackground.setBgColor(MinuetoColor.BLACK);
    Coordinate labelStart = new Coordinate(-100, -100);
    Coordinate labelSize = new Coordinate(0, 0);
    Label jonathan = new Label(labelStart, labelSize, "Jonathan Pullano");
    jonathan.setColor(MinuetoColor.WHITE);
    jonathan.setSize(new Coordinate(200, 100));
    Label application = new Label(labelStart, labelSize, "Application Logic");
    application.setColor(MinuetoColor.WHITE);
    application.setFontSize(50);
    Label wink = new Label(labelStart, labelSize, ";O");
    wink.setColor(MinuetoColor.WHITE);
    wink.setFontSize(50);
    Label marc = new Label(labelStart, labelSize, "Marc-Olivier Dozois Lyrette");
    marc.setColor(MinuetoColor.WHITE);
    marc.setSize(new Coordinate(200, 100));
    Label gfx = new Label(labelStart, labelSize, "Graphics");
    gfx.setColor(MinuetoColor.WHITE);
    gfx.setFontSize(50);
    Label bleh = new Label(labelStart, labelSize, "yay!");
    bleh.setColor(MinuetoColor.YELLOW);
    bleh.setFontSize(50);
    Label jeff = new Label(labelStart, labelSize, "Jeff Wallace");
    jeff.setColor(MinuetoColor.WHITE);
    jeff.setSize(new Coordinate(200, 100));
    Label network = new Label(labelStart, labelSize, "Network");
    network.setColor(MinuetoColor.WHITE);
    network.setFontSize(50);
    Label amazing = new Label(labelStart, labelSize, ";O");
    amazing.setColor(MinuetoColor.WHITE);
    amazing.setFontSize(50);
    Label empty = new Label(labelStart, labelSize, "");
    Coordinate scrollStart = new Coordinate(Application.WIN_SIZE_X / 2 - jonathan.getSize().getX() / 2, -jonathan.getSize().getY());
    Coordinate scrollEnd = new Coordinate(scrollStart.getX(), Application.WIN_SIZE_Y / 2 - jonathan.getSize().getY() / 2);
    Coordinate animEnd = new Coordinate(Application.WIN_SIZE_X, scrollEnd.getY());
    // Do the animations
    move(scrollStart, scrollEnd, jonathan, 3000);
    spinWeird(2 * Math.PI, jonathan, 2000);
    spin(Math.PI, jonathan, 1000);
    spin(Math.PI, jonathan, 500);
    spin(2 * Math.PI, jonathan, 500);
    spin(2 * Math.PI, jonathan, 250);
    roll(scrollEnd, animEnd, jonathan, Math.PI / 100, 1000);
    drawPause(scrollEnd, Math.PI / 6, application, 500);
    application.setFontSize(60);
    drawPause(scrollEnd, -Math.PI / 6, application, 500);
    application.setFontSize(70);
    drawPause(scrollEnd, Math.PI / 4, application, 500);
    application.setFontSize(80);
    drawPause(scrollEnd, -Math.PI / 4, application, 500);
    application.setFontSize(150);
    drawPause(new Coordinate(0, 0), Math.PI / 4, application, 500);
    drawPause(scrollEnd, 0, empty, 500);
    drawPause(scrollEnd, 0, wink, 250);
    drawPause(scrollEnd, 0, empty, 500);
    move(scrollStart, scrollEnd, marc, 3000);
    spinWeird(2 * Math.PI, marc, 2000);
    spin(Math.PI, marc, 1000);
    spin(Math.PI, marc, 500);
    spin(2 * Math.PI, marc, 500);
    spin(2 * Math.PI, marc, 250);
    roll(scrollEnd, animEnd, marc, Math.PI / 100, 1000);
    drawPause(scrollEnd, Math.PI / 6, gfx, 500);
    application.setFontSize(60);
    drawPause(scrollEnd, -Math.PI / 6, gfx, 500);
    application.setFontSize(70);
    drawPause(scrollEnd, Math.PI / 4, gfx, 500);
    application.setFontSize(80);
    drawPause(scrollEnd, -Math.PI / 4, gfx, 500);
    application.setFontSize(150);
    drawPause(new Coordinate(0, 0), Math.PI / 4, gfx, 500);
    drawPause(scrollEnd, 0, empty, 500);
    drawPause(scrollEnd, 0, bleh, 250);
    drawPause(scrollEnd, 0, empty, 500);
    move(scrollStart, scrollEnd, jeff, 3000);
    spinWeird(2 * Math.PI, jeff, 2000);
    spin(Math.PI, jeff, 1000);
    spin(Math.PI, jeff, 500);
    spin(2 * Math.PI, jeff, 500);
    spin(2 * Math.PI, jeff, 250);
    roll(scrollEnd, animEnd, jeff, Math.PI / 100, 1000);
    drawPause(scrollEnd, Math.PI / 6, network, 500);
    application.setFontSize(60);
    drawPause(scrollEnd, -Math.PI / 6, network, 500);
    application.setFontSize(70);
    drawPause(scrollEnd, Math.PI / 4, network, 500);
    application.setFontSize(80);
    drawPause(scrollEnd, -Math.PI / 4, network, 500);
    application.setFontSize(150);
    drawPause(new Coordinate(0, 0), Math.PI / 4, network, 500);
    drawPause(scrollEnd, 0, empty, 500);
    drawPause(scrollEnd, 0, amazing, 250);
    drawPause(scrollEnd, 0, empty, 500);
    // TODO: ADD OTHER NAMES HERE!
    Application.aManager.setGameState(new MainMenuState());
}
